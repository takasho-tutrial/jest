sudo: false
language: node_js
node_js: stable
cache:
  yarn: true
  directories:
  - node_modules
env:
  matrix:
  - CMD=test
  - CMD=lint
before_script:
- npx flow-typed install
script:
- npm run $CMD
